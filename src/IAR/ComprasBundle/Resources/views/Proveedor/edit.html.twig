{% extends 'IARMainBundle:Admin:layout.html.twig' %}

{% block main -%}
    <h1>Proveedor edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_proveedor') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
